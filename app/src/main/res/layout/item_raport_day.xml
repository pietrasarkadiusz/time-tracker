<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <data>
        <variable name="dayStats"
            type="com.example.timespenttracker.model.DayStats"/>
    </data>
    <com.google.android.material.card.MaterialCardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="24dp"
        app:cardCornerRadius="8dp"
        app:cardElevation="6dp"
        app:cardUseCompatPadding="true">
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingVertical="10dp"
            android:paddingHorizontal="16dp">
            <TextView
                android:text="@{dayStats.date}"
                style="@style/normal_text"/>
            <LinearLayout
                android:gravity="center_vertical"
                android:orientation="horizontal"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp">
                <TextView
                    android:text="@string/apps_usage"
                    style="@style/secondary_text"/>
                <RelativeLayout
                    android:id="@+id/usageLayout"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content">
                    <TextView
                        android:text="@{dayStats.usageInHMSFormat}"
                        style="@style/secondary_text"
                        android:layout_marginStart="2dp"/>
                    <TextView
                        android:textColor="@{dayStats.usagePercentChange>0 ? @color/red : @color/green}"
                        android:text="@{dayStats.usagePercentChangeString}"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_alignParentEnd="true"
                        android:textStyle="bold"
                        style="@style/secondary_text"/>
                </RelativeLayout>
            </LinearLayout>
            <LinearLayout
                android:gravity="center_vertical"
                android:orientation="horizontal"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="6dp">
                <TextView
                    android:text="@string/app_launches"
                    style="@style/secondary_text"/>
                <RelativeLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content">
                    <TextView
                        android:text="@{String.valueOf(dayStats.visits)}"
                        style="@style/secondary_text"
                        android:layout_marginStart="2dp"/>
                    <TextView
                        android:textColor="@{dayStats.visitPercentChange>0 ? @color/red : @color/green}"
                        android:text="@{dayStats.visitPercentChangeString}"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_alignParentEnd="true"
                        android:textStyle="bold"
                        style="@style/secondary_text"/>
                </RelativeLayout>
            </LinearLayout>
        </LinearLayout>
    </com.google.android.material.card.MaterialCardView>
</layout>